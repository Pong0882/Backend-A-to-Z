# Context 09 — 2026-04-20

## 오늘 한 것

### day11 (2026-04-20) — React 프론트엔드 초기 구축 (Issue #45)

**프론트엔드 세팅:**
- `pong-to-rich/frontend/` — Vite + React + React Router v6 + Tailwind CSS + Axios + lightweight-charts v5
- 로컬 개발: `npm run dev` (localhost:5173) — Vite 프록시로 Spring Boot :8080 연결 (CORS 우회)
- 배포 방향: Docker 멀티스테이지 빌드 → Nginx 서빙 (pong-server에 같이 올리기)

**구현된 페이지:**

| 경로 | 페이지 | 연결된 API |
|------|--------|-----------|
| `/` | 랜딩 | — |
| `/login` | 로그인 | POST /api/auth/login |
| `/signup` | 회원가입 | POST /api/auth/signup |
| `/stocks` | 주식 시세 | GET /api/stocks, GET /api/stocks/{code}, GET /api/stocks/{code}/prices, POST /api/stocks/{code}/fetch |
| `/watchlist` | 관심 종목 | GET/POST /api/watchlist, PATCH/DELETE /api/watchlist/{id} |
| `/portfolio` | 포트폴리오 | GET /api/portfolio, PATCH /api/portfolio/holdings/{id}/toggle-hidden |
| `/strategies` | 전략 | GET/POST /api/strategies, PATCH /api/strategies/{id}/{action}, DELETE /api/strategies/{id} |
| `/orders` | 주문 내역 | GET/POST /api/orders, PATCH /api/orders/{id}/cancel |
| `/broker-accounts` | 계좌 관리 | GET/POST /api/broker-accounts, DELETE /api/broker-accounts/{id} |

**핵심 파일 구조:**
```
pong-to-rich/frontend/
├── src/
│   ├── api/axios.js          ← Axios 인스턴스 + 인터셉터 (토큰 자동 첨부 + 만료 시 refresh)
│   ├── components/Navbar.jsx ← 로그인 상태에 따라 메뉴 분기
│   ├── pages/
│   │   ├── Landing.jsx
│   │   ├── Login.jsx
│   │   ├── Signup.jsx
│   │   ├── Stocks.jsx        ← 캔들차트 + 거래량 차트 + 일봉 수집 폼
│   │   ├── Watchlist.jsx
│   │   ├── Portfolio.jsx
│   │   ├── Strategies.jsx    ← 전략 생성 폼 (계좌 선택 드롭다운)
│   │   ├── Orders.jsx        ← 수동 주문 생성 폼 (지정가/시장가)
│   │   └── BrokerAccounts.jsx
│   ├── App.jsx               ← BrowserRouter + Routes
│   └── main.jsx
├── vite.config.js            ← /api, /swagger-ui, /v3/api-docs 프록시 → :8080
├── tailwind.config.js        ← 커스텀 색상 (surface/card/border/muted/up/down/primary)
└── .gitignore                ← node_modules, dist, .env 제외
```

**버그 수정:**
- `import_stock_csv.py` — `dtype={'stock_code': str}` 추가 (pandas가 숫자로 파싱해서 앞자리 0 손실하던 문제)

---

## 현재 상태

- `pongtrader.pro` 로 외부 접속 가능한 상태
- Spring Boot + MySQL + Redis Docker로 실행 중 (pong-server VM 안)
- Cloudflare 터널 상시 실행 중 (systemd)
- Grafana: `grafana.pongtrader.pro` 접속 가능
- **프론트: 로컬 개발 중 (미배포) — feat/issue-45-frontend 브랜치**
- **IntelliJ Build and run using → IntelliJ IDEA로 설정 (Gradle 아님)**

## VM 구성

```
192.168.100.1   — Windows 호스트
192.168.100.10  — pong-server  (Spring Boot + MySQL + Redis + Promtail, 4GB)
192.168.100.20  — monitoring-server (Prometheus + Grafana + Loki, 1.5GB)
192.168.100.66  — k6-server (부하 테스트 전용, 1GB)
```

## 배포 구조 계획 (미완성 — Issue #46, #47)

```
pongtrader.pro         → Nginx → React 빌드 결과물 (Docker)
api.pongtrader.pro     → Nginx → Spring Boot :8080 (Docker)
grafana.pongtrader.pro → Grafana (현재 그대로)
```

- 프론트/백 서브도메인 분리: Issue #46
- 프론트 CI/CD: Issue #47
- VM 추가 없이 pong-server 하나에서 Nginx가 두 도메인 처리

---

## 현재 구현된 API

| 메서드 | 경로 | 설명 | 인증 |
|--------|------|------|------|
| POST | /api/auth/signup | 회원가입 | X |
| POST | /api/auth/login | 로그인 | X |
| POST | /api/auth/refresh | Access Token 재발급 | X |
| POST | /api/auth/logout | 로그아웃 | O |
| GET | /api/stocks | 종목 목록 조회 | X |
| GET | /api/stocks/{code} | 현재가 (KIS 실시간) | X |
| GET | /api/stocks/{code}/prices | 일봉 데이터 조회 (전체 — 페이지네이션 미적용) | X |
| POST | /api/stocks/{code}/fetch | 기간별 일봉 수집 → DB 저장 | X |
| POST | /api/broker-accounts | 증권사 계좌 등록 | O |
| GET | /api/broker-accounts | 내 계좌 목록 | O |
| GET | /api/broker-accounts/{id} | 계좌 단건 | O |
| DELETE | /api/broker-accounts/{id} | 계좌 비활성화 | O |
| POST | /api/watchlist | 관심 종목 등록 | O |
| GET | /api/watchlist | 관심 종목 목록 | O |
| PATCH | /api/watchlist/{id} | 알림가 수정 | O |
| DELETE | /api/watchlist/{id} | 관심 종목 삭제 | O |
| POST | /api/strategies | 전략 생성 | O |
| GET | /api/strategies | 전략 목록 | O |
| GET | /api/strategies/{id} | 전략 단건 | O |
| PATCH | /api/strategies/{id}/activate | 전략 활성화 | O |
| PATCH | /api/strategies/{id}/pause | 전략 일시정지 | O |
| PATCH | /api/strategies/{id}/deactivate | 전략 중지 | O |
| DELETE | /api/strategies/{id} | 전략 삭제 | O |
| POST | /api/orders | 수동 주문 생성 | O |
| GET | /api/orders | 주문 목록 | O |
| GET | /api/orders/{id} | 주문 단건 | O |
| PATCH | /api/orders/{id}/cancel | 주문 취소 | O |
| GET | /api/portfolio | 포트폴리오 조회 | O |
| PATCH | /api/portfolio/holdings/{id}/toggle-hidden | 보유 종목 숨김 토글 | O |

---

## 오늘 완료한 항목

- [x] Issue #45 — React 프론트엔드 초기 구축
- [x] import_stock_csv.py 종목코드 앞자리 0 손실 버그 수정

---

## 결정한 것들

- [2026-04-20] 프론트 VM 분리 안 함 — MSA 전환 시점에 자연스럽게 분리 예정. 지금은 pong-server 하나에서 Nginx가 처리
- [2026-04-20] 프론트 레포 분리 안 함 — `pong-to-rich/frontend/` 모노레포 유지
- [2026-04-20] 서브도메인: `api.pongtrader.pro` (백엔드), `pongtrader.pro` (프론트)
- [2026-04-20] 프론트 배포: Docker 멀티스테이지 빌드 → Nginx 서빙 (백엔드와 동일 방식)
- [2026-04-20] lightweight-charts v5 API 변경 — `addCandlestickSeries()` → `addSeries(CandlestickSeries, options)`

---

## 다음에 할 것

- feat/issue-45-frontend → main PR 머지
- Issue #46 — api.pongtrader.pro 서브도메인 분리 (Cloudflare DNS + Nginx + CORS)
- Issue #47 — 프론트엔드 CI/CD (GitHub Actions + Nginx 배포 자동화)
- Issue #48 — GET /api/stocks/{code}/prices 페이지네이션 + k6 성능 비교
- Issue #49 — 증권사 계좌 등록 시 KIS API 유효성 검증
- Issue #32 — KIS 실제 주문 전송 연동
- Issue #33 — 포트폴리오 평가손익 계산
- 서버 타임존 UTC → KST 수정
- DB stock_prices 종목코드 앞자리 0 손실 데이터 재삽입 (DELETE 후 import_stock_csv.py 재실행)
